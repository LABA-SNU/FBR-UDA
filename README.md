# FBR-UDA (code-only)

This repository contains code and structure only. **No data or weights are included.**

Expected data layout:
data/
├── apple/
│   ├── PV/images/
│   └── plantpathology/cropped_bg/
└── test/

Put your own data under `data/` (git-ignored).
